import 'package:flutter/material.dart';
import 'package:shared/shared.dart';

Widget calendarNotificationBuilder(BuildContext context) {
  return const SizedBox.shrink();
  return Container(padding: const EdgeInsets.all(24), color: Colors.amber, child: const AppText('Notification'));
}
